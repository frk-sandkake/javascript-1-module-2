// play

const animal = ["dog", "mouse", "cat"];

console.log(animal.length);

for (let i = 0; i < animal.length; i++) {
    console.log(animal[i]);
}

const name = "   christina  ";
console.log("before: ", name);

const trimName = name.trim();
console.log("after: ", trimName);